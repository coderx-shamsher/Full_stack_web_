MySQL CLI mein table ka **structure** dekhne ke liye:

```sql
DESCRIBE users;
```

ya short form:

```sql
DESC users;
```

Alternative:

```sql
SHOW CREATE TABLE users;
```

👉 `DESC users;` → columns, datatype, key, NULL, default etc. quickly dikhata hai.
